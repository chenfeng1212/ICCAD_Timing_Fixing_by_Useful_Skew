#ifndef SPFA_H
#define SPFA_H

#include "ds.h"

namespace skew {
    namespace SPFA {
        inline void spfa(DesignDB db){
            std::vector<int> times;
            for(auto const db.ffs : FF){

            }
            db.buildConstraintGraph(ConstraintKind::SETUP);
            times.assign(db.ffs.size(),0);

        }
    }
}






#endif